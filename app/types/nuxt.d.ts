declare module '#app' {
    interface PageMeta {
        pageTitle?: string
        /** If true, inner-page layout main column scrolls vertically */
        scrollMainContent?: boolean
    }
}

export {}
